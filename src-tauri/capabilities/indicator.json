{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "indicator",
  "description": "Capability for the click-through recording indicator overlay window",
  "windows": ["indicator"],
  "permissions": [
    "core:event:default",
    "core:window:allow-hide"
  ]
}
